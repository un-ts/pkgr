checks:
  - name: build-size-watcher
    options:
      files:
        - path: 'index.*'
  - name: type-coverage-watcher
    options:
      ignoreCatch: true
      ignoreFiles:
        - '*.d.ts'
      strict: true
